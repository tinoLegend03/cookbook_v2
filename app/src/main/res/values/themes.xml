<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RoomDB_Replica_Ufficiale"
        parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Colore della status-bar (API 21+) -->
        <item name="android:statusBarColor">@color/blu1</item>

        <!-- Fallback per librerie che usano ancora colorPrimaryDark -->
        <item name="colorPrimaryDark">@color/blu1</item>

        <!-- Icône di sistema scure su sfondo chiaro -->
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>